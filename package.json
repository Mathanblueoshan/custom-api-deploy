{
  "name": "custom-api-deploy",
  "version": "1.0.0",
  "description": "create custom api with mongoDb Atlas(cloud DB) and deploy(render.com)",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js"
  },
  "author": "mathan kumar",
  "license": "ISC",
  "dependencies": {
    "custom-api-deploy": "file:",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.9.6"
  }
}
